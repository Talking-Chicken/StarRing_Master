using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Michsky.DreamOS;
using TMPro;

public class LaptopMonitor : MonoBehaviour
{
    [SerializeField] PhotoGalleryManager photoApp;
    [SerializeField] TextMeshProUGUI photoviewTitle;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (photoviewTitle.text=="" )
        {
            
        }
    }
}
